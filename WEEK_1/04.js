// 불리언(Boolean)
// true(참), false(거짓)
let bool = true;
let bool2 = false;
// console.log(bool, bool2)

// if : 만약~~ 한다면~~~

// undefined
// un : not / define:정의하다.
//변수선언하고 값 안주면 됨.
let x;
console.log(x)

//null : 값이 존재하지 않음을 '명시적으로' 나타내는 방법
// null != undefined
let y = null;
console.log(y);

//object(객체) : key - value pair
let = person = {
    naem : "kim",
    age : 20,
    isMarried: true,
};

console.log(typeof person)

//array(배열)
//여러개의 데이터를 순서대로 저장하는 데이터 타입!!
//하나하나가 다 인덱스를 가지고 있읍니다..
let number = [1,2,3,4,5]
console.log(number)